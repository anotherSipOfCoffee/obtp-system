# OBTP System

Independent repository for the System project. Website files are in `dist/`.
Static HTML/CSS/JavaScript; no build or server required.

## Cloudflare
For a Git-integrated Pages project, use main as the production branch, no framework/build step, and dist as the output directory.
Current Direct Upload projects cannot switch to Git integration in place; retain the existing deployment until a migration is deliberately configured. Repository preparation does not change hosting.

## Status
Imported from OBTP Project System v74 into this public GitHub repository with owner approval. Website files are in dist/. Hosting migration and Cloudflare deployment remain pending.

System v02: eight definitions, two bay configurations, six conceptual interfaces. Run `node tests/kit.cjs` and `node tests/interface.cjs`. Read docs/SYSTEM_RULES.md before editing. No engineering validation claimed.
