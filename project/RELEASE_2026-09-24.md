# System-first cassette release

On 2026-09-24 the owner authorized publishing System first, validating both WikiHouse and Cassette 01, then publishing Studio v3 from that tested System revision. This supersedes the earlier no-deployment instruction for this release only. Preserve ordinary Git history, Studio v1 and WikiHouse. Architecture is outside this release; Drive remains the master baseline and is not automatically synchronized.

System names: v1 WikiHouse; v2 Cassette 01. Studio names: v1 original reference; v2 WikiHouse; v3 Cassette 01. These numbering schemes are local to each project.

The release workflow tests source geometry plus both browser applications before deployment, and both live applications after deployment. Studio must not be released until System's live tests pass. Studio will pin that tested System commit.

Drive baseline v76 remains unchanged. GitHub adds navigation labels, deployment gates and live checks. Engineering holds and disabled openings are unchanged.
