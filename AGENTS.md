# System working rules
Preserve the independent frame + panels direction, limited library and explicit provisional status. No supplier work or invented engineering validation. Read docs/SYSTEM_RULES.md. Geometry and quantities must come from dist/kit.js; do not hard-code counts in the interface. Test one and two bays before adding objects. Keep object identifiers stable. Never claim geometric contacts are solved joints. Keep Studio and Architecture unchanged unless specifically requested. Communicate in coherent batches and offer options only at meaningful decisions. Package/Drive changes only when asked. Preserve Sites identity until an explicit repository migration is completed; do not claim GitHub or Cloudflare sync without confirmed push/deployment.
